#!/bin/bash
echo "Running default /usr/libexec/s2i/assemble"

/usr/libexec/s2i/assemble
rc=$?

if [ $rc -eq 0 ]; then
    echo "After successful assembling running 'next build'"
    next build
else
    echo "After failed assembling"
fi

exit $?
