//
// import { selectPageProfileDomain } from '../selectors';

describe('selectPageProfileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
