/*
 * Onboarding reducer
 */

import {
} from './constants';

const initialState = {

};

function onboarding(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default onboarding;
